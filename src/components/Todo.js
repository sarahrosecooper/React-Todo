import React from "react";

const Todo = (props) => {
  return (
    <div>
      This is the Todo component that takes in the todo data and displays the
      task to the screen.
    </div>
  );
};

export default Todo;
